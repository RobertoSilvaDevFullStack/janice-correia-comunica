# .env.local - Configuração para desenvolvimento local com banco de produção

# ============================================
# INSTRUÇÕES PARA CONECTAR AO BANCO REMOTO
# ============================================
# 
# O hostname 'janiceportfolio_janicedb' é interno do Docker e não funciona externamente.
# Você precisa substituir pelo IP público ou domínio do servidor onde o PostgreSQL está rodando.
#
# Opções para descobrir o hostname correto:
# 1. Se o banco está no mesmo servidor que a API de produção, use o IP/domínio do servidor
# 2. Verifique se o PostgreSQL está exposto em uma porta pública (geralmente 5432)
# 3. Pode ser necessário configurar um túnel SSH se o banco não estiver exposto publicamente
#
# Formato: postgresql://usuario:senha@HOSTNAME_OU_IP:PORTA/nome_do_banco
# ============================================

# OPÇÃO 1: Conexão direta (se o PostgreSQL estiver exposto publicamente)
# Substitua 'SEU_SERVIDOR_IP_OU_DOMINIO' pelo IP ou domínio do servidor
DATABASE_URL=postgresql://postgres:Lone2970Sw1@SEU_SERVIDOR_IP_OU_DOMINIO:5432/janiceportfolio?sslmode=disable

# OPÇÃO 2: Se estiver usando Railway, Render, ou similar, use a URL externa fornecida
# DATABASE_URL=postgresql://postgres:senha@external-hostname.railway.app:5432/railway

# OPÇÃO 3: Túnel SSH (se o banco não estiver exposto)
# Primeiro crie um túnel: ssh -L 5432:janiceportfolio_janicedb:5432 usuario@servidor
# Depois use: DATABASE_URL=postgresql://postgres:Lone2970Sw1@localhost:5432/janiceportfolio?sslmode=disable

# Frontend URL
FRONTEND_URL=http://localhost:5173

# JWT Secret (use o mesmo de produção para compatibilidade)
JWT_SECRET=seu_jwt_secret_super_seguro_aqui_pelo_menos_32_caracteres_min
JWT_EXPIRES_IN=24h

# Server
PORT=3001
NODE_ENV=development

# CORS
CORS_ADDITIONAL_ORIGINS=
