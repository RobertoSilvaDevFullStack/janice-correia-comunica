# Database
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway

# JWT (IMPORTANTE: Use um secret forte com pelo menos 32 caracteres)
JWT_SECRET=seu_jwt_secret_super_seguro_aqui_pelo_menos_32_caracteres_min
JWT_EXPIRES=24h

# Server
PORT=3001
NODE_ENV=development
DB_SSL=disable # use 'disable' quando o Postgres não usa SSL (EasyPanel)

# CORS
FRONTEND_URL=http://localhost:5173
CORS_ADDITIONAL_ORIGINS=
